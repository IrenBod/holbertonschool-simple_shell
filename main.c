#include "shell.h"

/**
 * main - Entry point of the shell program.
 * This function initializes the shell loop.
 *
 * Return: Always returns 0 (success).
 */

int main(void)
{
	shell_loop();
	return (0);
}
